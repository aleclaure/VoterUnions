# Supabase Configuration (Legacy)
# These are used when EXPO_PUBLIC_USE_NEW_BACKEND=false
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Feature Flags (Blue Spirit Migration)
# Controls migration from Supabase to WebAuthn-based backend

# Email Verification Feature Flag
# true = Email verification required (default)
# false = Email verification disabled (development/testing)
EXPO_PUBLIC_REQUIRE_EMAIL_VERIFICATION=true

# WebAuthn Authentication Feature Flag
# true = Use WebAuthn (biometric/hardware key) authentication
# false = Use Supabase email/password authentication (default during migration)
EXPO_PUBLIC_USE_WEBAUTHN=false

# Backend API Feature Flag
# true = Use new microservices backend (privacy-first)
# false = Use Supabase backend (default during migration)
EXPO_PUBLIC_USE_NEW_BACKEND=false

# WebAuthn Rollout Percentage (0-100)
# Controls gradual rollout of WebAuthn authentication
# 0 = No users in rollout (default)
# 10 = 10% of users in rollout
# 100 = All users in rollout
EXPO_PUBLIC_WEBAUTHN_ROLLOUT_PERCENT=0

# New Backend API URL
# Base URL for the new microservices backend
# Only used when EXPO_PUBLIC_USE_NEW_BACKEND=true
# Development: http://localhost:3001
# Production: https://your-api-domain.com
EXPO_PUBLIC_API_URL=http://localhost:3001

# Session Configuration
# Used for JWT token expiration and session management
SESSION_SECRET=your_session_secret_here_change_in_production
