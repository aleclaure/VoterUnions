# Auth Service Environment Variables

# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/voter_unions_auth

# JWT Secrets (CHANGE IN PRODUCTION!)
JWT_SECRET=your-secret-key-min-32-characters-long
REFRESH_SECRET=your-refresh-secret-key-min-32-characters-long

# CORS
CORS_ORIGIN=http://localhost:8081

# ============================================================================
# Audit Logging - Secure Backend Audit with Encryption
# ============================================================================

# AES-256 Encryption Key for Audit Logs (64 hex characters = 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Keep this secret! Rotate every 90 days.
AUDIT_ENCRYPTION_KEY=your_64_character_hex_key_here_generate_with_crypto_randomBytes_32
